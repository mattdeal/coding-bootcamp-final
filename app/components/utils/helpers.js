var axios = require("axios");

// Helper functions for making API Calls
var helper = {
  getSurvey: function(surveyId) {},
  createSurvey: function(survey) {},
  getSurveys: function(userId) {}
};

// We export the API helper
module.exports = helper;
